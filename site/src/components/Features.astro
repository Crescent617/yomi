---
import Icon from './Icon.astro';

const features = [
  {
    icon: 'terminal',
    title: 'Terminal-first TUI',
    desc: 'A minimalist terminal interface for seamless interaction, plus a Tauri desktop app when you want one.',
  },
  {
    icon: 'wrench',
    title: 'Full toolbelt',
    desc: 'File read/write/edit, glob & grep, shell execution, web search — everything an agent needs, built in.',
  },
  {
    icon: 'plug',
    title: 'Any provider',
    desc: 'OpenAI, Anthropic, or any OpenAI-compatible endpoint. Configurable context window and agent tools.',
  },
  {
    icon: 'shield',
    title: 'Safe by default',
    desc: 'Read-only tool tiers, git-aware file walking, and confirmation for every mutation — unless you go YOLO.',
  },
  {
    icon: 'smartphone',
    title: 'Remote control',
    desc: 'Run the daemon anywhere and steer it from Feishu or Telegram — work continues away from the keyboard.',
  },
  {
    icon: 'server',
    title: 'Docker-ready daemon',
    desc: 'The official image boots straight into daemon mode with batteries included: git, ripgrep, tmux, and more.',
  },
] as const;
---

<section id="features" class="border-t border-line/70">
  <div class="mx-auto max-w-5xl px-6 py-24">
    <p class="micro-label text-primary">Features</p>
    <h2 class="mt-3 text-3xl font-bold tracking-tight">
      Everything you need, nothing you don't
    </h2>
    <div class="mt-12 grid gap-x-8 gap-y-10 sm:grid-cols-2 lg:grid-cols-3">
      {
        features.map(({ icon, title, desc }) => (
          <div>
            <Icon name={icon} class="h-5 w-5 text-primary" />
            <h3 class="mt-3 font-semibold">{title}</h3>
            <p class="mt-2 text-sm leading-relaxed text-muted">{desc}</p>
          </div>
        ))
      }
    </div>
  </div>
</section>
